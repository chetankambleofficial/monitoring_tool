# Database Configuration
DATABASE_URL=postgresql://postgres:123456@localhost:5432

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=5000

# Security
SECRET_KEY=change-this-to-a-random-secret-key

# Optional: CORS (if frontend is on different domain)
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# Optional: External Database (for production)
# DATABASE_URL=postgresql://user:password@remote-server.com:5432/sentineledge
